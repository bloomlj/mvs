extends ../layout
block content
    h1 全部投票记录
    a.btn(href="/vote/add") 
       i.icon-file
       | 添加投票/评价
    ul#votelist.thumbnails
        for doc in docs
           li.span6
            div.thumbnail
                h4 #{doc.name}#{doc.vtype}
                span.metainfo #{doc.created_date}
                .note #{doc.note}
                .actions
                    |操作：
                    a.btn(href="/vote/#{doc._id}/edit") 
                        i.icon-pencil
                        |修改
                    a.btn(href="/vote/#{doc._id}/destroy") 
                        i.icon-remove
                        |删除
                    a.btn(href="/vote/#{doc._id}/result") 
                        i.icon-list
                        |查看结果
                    form(action="/vote/#{doc._id}",method="post")
                        input(type="hidden",name="_method",value="put")
                        if doc.isopen == 'no'
                            input(type="hidden",name="isopen",value="yes")
                            input.btn.btn-inverse(type="submit",value="启动")
                        else
                            input(type="hidden",name="isopen",value="no")
                            input.btn.btn-danger(type="submit",value="停止")                        
