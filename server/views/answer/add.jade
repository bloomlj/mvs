extends ../layout
block content
  h1 添加投票/评价组

  form(method="post",action="/answer")
    p
      label(for="name") 方案名称
    p
      input#name(name="name",type="text")
    p
      label(for="note") 方案描述
    p
      textarea#note(name="note")
    p
      label(for="name") 方案名称
    p
      input#name(name="name",type="text")
    p
      label(for="note") 方案描述
    p
      textarea#note(name="note")
    p 
      input(type="submit")
